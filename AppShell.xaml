<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="NmapMaui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:NmapMaui.Views"
    xmlns:local="clr-namespace:NmapMaui"
    Shell.TabBarIsVisible="False">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="LoginPage"
        FlyoutItemIsVisible="False" />

    
    <FlyoutItem Title="Dashboard" Icon="home.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Network Tools" Icon="network.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:NetworkToolsPage}"
            Route="NetworkToolsPage" />
    </FlyoutItem>

    <FlyoutItem Title="Crypto Tools" Icon="lock.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:CryptographyToolsPage}"
            Route="CryptographyToolsPage" />
    </FlyoutItem>
    
    <FlyoutItem Title="Password Tools" Icon="key.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:PasswordToolsPage}"
            Route="PasswordToolsPage" />
    </FlyoutItem>

    <FlyoutItem Title="Database Control" Icon="database.png">
        <ShellContent
            ContentTemplate="{DataTemplate local:DatabaseControlPage}"
            Route="DatabaseControlPage" />
    </FlyoutItem>
    
    <FlyoutItem Title="Change Password" Icon="key.png">
        <ShellContent
            ContentTemplate="{DataTemplate views:ChangePasswordPage}"
            Route="ChangePasswordPage" />
    </FlyoutItem>

    <FlyoutItem Title="Logout" Icon="logout.png">
        <ShellContent
            ContentTemplate="{DataTemplate views:LoginPage}"
            Route="LogoutPage" />
    </FlyoutItem>

    <Shell.FlyoutHeader>
        <Grid BackgroundColor="#181A20" HeightRequest="200">
            <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
                <Label Text="Security Tools"
                       FontSize="24"
                       FontAttributes="Bold"
                       TextColor="#00BFAE"/>
                <Label Text="Network &amp; Crypto Tools"
                       FontSize="16"
                       TextColor="#00BFAE"/>
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

</Shell>
