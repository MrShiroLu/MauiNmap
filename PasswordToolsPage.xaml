<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NmapMaui.PasswordToolsPage">

    <Grid RowDefinitions="Auto,*" Padding="25" BackgroundColor="Transparent">
        <!-- Header -->
        <VerticalStackLayout Grid.Row="0" HorizontalOptions="Center" Margin="0,10,0,30" Spacing="5">
            <Label Text="> PASSWORD_SECURITY_UTILITIES"
                   TextColor="{StaticResource Primary}"
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>
            <Label Text="Entropy-based generators, strength estimators &amp; data breach auditors"
                   TextColor="{StaticResource TextSecondary}"
                   FontSize="13"
                   HorizontalOptions="Center"/>
        </VerticalStackLayout>

        <!-- Cards -->
        <Grid Grid.Row="1"
              RowDefinitions="Auto"
              ColumnDefinitions="*,*,*"
              ColumnSpacing="20">
            
            <!-- Password Generator -->
            <Frame Grid.Row="0" Grid.Column="0">
                <VerticalStackLayout Spacing="12" VerticalOptions="Center">
                    <Label Text="[+] CREDENTIAL_GEN"
                           TextColor="{StaticResource Primary}"
                           FontSize="18"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>
                    <Label Text="Create high-entropy passwords with custom length and charset filters."
                           TextColor="{StaticResource TextSecondary}"
                           FontSize="13"
                           HorizontalTextAlignment="Center"
                           HorizontalOptions="Center"/>
                    <Button Text="EXECUTE"
                            Clicked="OnPasswordGenPageClicked"
                            Margin="0,10,0,0"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Password Strength -->
            <Frame Grid.Row="0" Grid.Column="1">
                <VerticalStackLayout Spacing="12" VerticalOptions="Center">
                    <Label Text="[+] STRENGTH_AUDIT"
                           TextColor="{StaticResource Primary}"
                           FontSize="18"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>
                    <Label Text="Analyze target password complexity and estimated time-to-crack."
                           TextColor="{StaticResource TextSecondary}"
                           FontSize="13"
                           HorizontalTextAlignment="Center"
                           HorizontalOptions="Center"/>
                    <Button Text="EXECUTE"
                            Clicked="OnPasswordStrengthPageClicked"
                            Margin="0,10,0,0"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Password Leak Lookup -->
            <Frame Grid.Row="0" Grid.Column="2">
                <VerticalStackLayout Spacing="12" VerticalOptions="Center">
                    <Label Text="[+] LEAK_AUDITOR"
                           TextColor="{StaticResource Primary}"
                           FontSize="18"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>
                    <Label Text="Check if credentials have been compromised in known public breaches."
                           TextColor="{StaticResource TextSecondary}"
                           FontSize="13"
                           HorizontalTextAlignment="Center"
                           HorizontalOptions="Center"/>
                    <Button Text="EXECUTE"
                            Clicked="OnPasswordLeakCheckPageClicked"
                            Margin="0,10,0,0"/>
                </VerticalStackLayout>
            </Frame>
        </Grid>
    </Grid>
</ContentPage>
